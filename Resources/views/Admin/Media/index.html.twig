{% extends 'WellCommerceAppBundle:Admin:layout.html.twig' %}
{% trans_default_domain "wellcommerce" %}
{% block content %}
    <h2>{% trans %}media.heading.index{% endtrans %}</h2>
    <script type="text/javascript">
        function processMedia(row) {
            var icon =
            row.preview = '<a title="'+ row.name +'" href="' + row.preview + '" class="show-thumb"><img src="{{ asset('bundles/wellcommerceapp/images/icons/datagrid/details.png') }}" style="vertical-align: middle;" /></a>';
            return row;
        }

        function dataLoadedMedia(dDg) {
            dDg.m_jBody.find('.show-thumb').mouseenter(GTooltip.ShowThumbForThis).mouseleave(GTooltip.HideThumbForThis);
        }
    </script>
    {{ datagrid_renderer(datagrid) }}
{% endblock %}
